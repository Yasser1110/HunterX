Drop user-authored Nuclei templates here (written only for targets you are
authorized to test). Huntrex runs these only against scope-validated assets
and only the template categories enabled in config (safe/exposure/misconfig/cve
by default; aggressive is off unless explicitly enabled).